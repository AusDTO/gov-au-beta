%ul.breadcrumbs
  - @node.parents.reverse.each do |parent|
    %li
      %a{href: node_path(parent)}= parent.name

%p= "#{@node.id} - #{@node.name}"
%p= @node.source_url
%p= @node.content

%ul
  - @node.children.each do |node|
    %li
      %a{href: node_path(node)}= node.name
