!!! 5

%html
  = render 'head', title: 'GovAuBeta: Collaboration'
  %body
    %header{role: 'banner'}
      = render 'application/signed_in_nav'
      %section.govau--header
        %div.wrapper
          %div.govau--logo
            %a.logo(href="/")
              gov.au
            %span.badge--beta beta

    %nav.breadcrumbs{'aria-label' => 'breadcrumb'}
      .wrapper
        = breadcrumbs style: :ul, class: nil

    %main
      - if content_for?(:navbar)
        %aside.sidebar
          %nav.primary-nav{'aria-label' => 'main navigation'}
            = yield :navbar
      %article
        = render 'alerts'
        = yield
    %footer{role: "contentinfo"}
      %div.wrapper
        %p
          &lt;insert footer here&gt;
