- if depth <= NodesHelper::MAX_MENU_DEPTH
  %ul
    - nodes.each do |node|
      %li
        = link_to(node, nodes_path(node.path))
        = render partial: "menu", locals: {nodes: node.children.published, depth: depth += 1, section: section}
