- if depth < 5
  %ul
    - nodes.each do |node|
      %li
        = link_to(node, nodes_path(section: section, path: node.path))
        = render partial: "menu", locals: {nodes: node.children, depth: depth += 1, section: section}
