%table.table-alternating
  %thead
    %tr
      %th Name
      %th Create Date
  %tbody
    - submissions.each do |submission|
      %tr
        %td= link_to "#{submission.revisable.name}", editorial_submission_path(submission)
        %td #{submission.submitted_at}