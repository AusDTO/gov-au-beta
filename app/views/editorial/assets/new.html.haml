= content_for :title do
  = "Create a asset"

- breadcrumb :new_asset

%h1 Create a asset
%p Upload an image for use on GOV.AU

%div.form_wrapper
  = simple_form_for ([:editorial, @form]) do |f|
    = f.input :asset_file, as: :file
    = f.button :button, 'Create asset'