%article.verify
  %h1 Google Authenticator setup

  %h2 Step 1
  %p
    Enter this code into your authenticator app

  .callout
    %h3= @totp_secret

  %section.sign-in-form
    .sign-in-footer-links
      .background-border-heading
        %span.inner
          Or

  .qrcode
    Scan this code directly in to your authenticator app
    = image_tag "#{@qrcode.to_data_url}"

  = link_to 'Generate a new code', resend_code_users_two_factor_setup_authenticator_path, method: :post

  %h2 Step 2
  %p
    Enter the code generated by your authenticator app

  %section.sign-in-form
    = form_tag([:users, :two_factor_setup_authenticator], method: :post) do
      - if @form_error
        =@form_error
      = label_tag 'code', 'Enter 6 digit code'
      = text_field_tag  :code, nil, autocomplete: :off

      .form-footer-actions
        .form-options
          %p
            = submit_tag 'Verify', role: :button
