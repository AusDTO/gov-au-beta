-# locals: node
- depth = node.options.toc
- if depth > 0
  %nav.index-links
    %h2 On this page
    -# Add one to depth because nesting_level refers to the markdown heading level
    -# and content should start at h2
    != node.rendered[:content_body].toc(depth + 1)
